cd weather-project || git clone https://github.com/Mohamed393652/Weather-app.git weather-project
cd weather-project
git fetch --all
git reset --hard origin/main

sudo docker compose down
sudo docker compose build --no-cache
sudo docker compose up -d
